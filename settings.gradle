pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        jcenter() // Eğer gerekli bağımlılık JCenter üzerinden geliyorsa
    }
}

rootProject.name = "NetFood"
include ':app'
